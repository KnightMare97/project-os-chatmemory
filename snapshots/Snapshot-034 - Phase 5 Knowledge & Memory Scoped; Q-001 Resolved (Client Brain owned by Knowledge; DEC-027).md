# Snapshot-034 - Phase 5 Knowledge & Memory Scoped; Q-001 Resolved (Client Brain owned by Knowledge; DEC-027)

## Current Phase
Phase 11 Claude Code Operating System — operational
Phase 5 Knowledge & Memory Architecture — **scoped (DEC-027 question-gate closed)**;
`memory.md` first write pending as landing (ii)
Phase 1 Domain Discovery — **Q-001 resolved** (Client Brain owned by Knowledge,
DEC-027); the ownership update is an isolated, explicitly-called-out `domains.md`
edit
Phase 4 Extensibility Model — first write complete (Snapshot-030), unchanged
Phase 3 Capability Map — written (Snapshot-025), unchanged
Phase 2 Experience Architecture — complete (7-of-7, Snapshot-033); four Client
Brain citations refreshed to DEC-027 (reference altitude, R-028)

## Current Topic
The Phase 5 Knowledge & Memory question-gate (G-1 → G-7), run inline in grill-me
form and closed in DEC-027. The gate's core decision (G-6) resolves Q-001: Client
Brain is owned by the Knowledge domain. This snapshot records the close-out at
reference altitude.

## Status
DEC-027 is landed and verified on origin/main across two commits:
- **`e44a201`** (landing (i)) — DEC-027 + the isolated `domains.md` ownership update
  (13 sites) + tracker backfill (open-questions.md Q-001/Q-004) + the gate brainstorm
  capture, atomic.
- **`4550d46`** (knock-on) — `experience-architecture.md` four Client Brain
  citations refreshed to the new truth (reference altitude only, R-028).

Working tree clean. An independent verifier ran twice — once pre-landing (which
surfaced four ownership-assertion sites beyond the initial enumeration, including
two joint-ownership statements) and once on the exact staged set (10/10 checks PASS,
grep-complete, no surviving ownership-openness contradiction).

**Q-001 fully resolved.** Client Brain is owned by Knowledge, as a Memory Object /
Shared Service Artifact; Client Success contributes relationship-relevant content
but does not own it; CRM references it but does not own it. The ownership truth is
made concrete in `domains.md` (Knowledge "What it owns" gains Client Brain; the
Client Brain section's ownership block becomes resolved; the Draft-v2 "Primary
Owner" and Proposed-Model joint-ownership lines corrected; the Knowledge↔Client
Success boundary settled; in-file Open-Question echoes marked resolved; the DEC-026
slice resource list refreshed).

**Carried, not resolved:** Q-004 (Client Brain partitioning — per Client / per Brand
/ both), entangled with Q-003 Brand placement; the `memory.md` Client Brain entry
will be written partition-agnostic. The G-7 insight→durable-knowledge threshold
(`domains.md:1917`) stays Phase-1 Intelligence truth, pending (R-027). Aggregate
status stays draft.

---

## Document Updates

### DOC-079
`decisions.md` — **DEC-027 added** (Active): Client Brain ownership resolved to
Knowledge; full Phase 5 gate outcome (G-1–G-7), two-landing plan, holds, tracker
plan. Placed before the Supersession Rule.

### DOC-080
`Faraz-OS-Canon/domains.md` — Client Brain ownership resolved to Knowledge across 13
sites (commit `e44a201`): the Knowledge "What it owns" insertion; the Client Brain
section ownership block; the Draft-v2 "Primary Owner" and Proposed-Model
joint-ownership corrections; the Knowledge↔Client Success boundary line; two in-file
Open-Question echoes; the DEC-026 slice resource list; the catalog "direction" lines
(×2); the Shared Service example; the Knowledge risk wording; the CRM open-question
sub-bullet. Q-004 / Q-003 / aggregate status untouched; `:2457` access-only rule
untouched.

### DOC-081
`Faraz-OS-Canon/experience-architecture.md` — four Client Brain citations refreshed
to "owned by Knowledge per DEC-027; partitioning draft Q-004" (commit `4550d46`;
reference altitude only, R-028): `:191`, `:256`, `:549`, `:713`.

### DOC-082
`open-questions.md` — **Q-001 marked Resolved/Closed** (DEC-027); **Q-004 note
added** (partitioning stays open, entangled with Q-003; entry written
partition-agnostic). Same commit as the `domains.md` landing.

### DOC-083
`findings.md` — **FIND-031 backfilled** (same close-out) recording the
grep-completeness lesson.

### DOC-084
`Current-State.md` — Current Architecture Position adds Phase 5 scoped; Current
Important Draft Areas updates "final Client Brain ownership" (ownership resolved;
partitioning Q-004 remains); Current Next Focus leads with Phase 5
(`memory.md` pending as landing (ii)).

### DOC-085
`brainstorms/2026-06-08-phase5-knowledge-memory-gate.md` — created: the Phase 5
question-gate capture (G-1 → G-7, riders, knock-on reconciliation, Ali's calls).

---

## Decisions

### DEC-027 (new, Active)
Client Brain ownership resolved: Client Brain is owned by the Knowledge domain, as a
Memory Object / Shared Service Artifact; Client Success contributes but does not
own; CRM references but does not own. Resolves **Q-001**. Carries Q-004
(partition-agnostic rider) and the G-7 insight→durable-knowledge threshold (R-027).
Closes the Phase 5 question-gate and frames the `memory.md` write. See `decisions.md`.

---

## Findings

### FIND-031 (new)
Resolving a long-standing draft echoed across a canon file requires a grep-complete
reconciliation sweep, not the initially-named edit sites. DEC-027's first knock-on
enumeration was incomplete; the independent verifier's full grep surfaced four more
ownership-assertion sites (two asserting joint ownership) that would have left
`domains.md` self-contradicting the resolution. The verification pass caught it
before commit. Treat the verifier's grep-completeness check as a landing gate. See
`findings.md`.

---

## Open Questions
- **Q-001 — RESOLVED** (DEC-027); closed in open-questions.md.
- **Q-004 — open (carried):** Client Brain partitioning (per Client / per Brand /
  both); entangled with Q-003 Brand placement; `memory.md` entry written
  partition-agnostic.
- **Q-003 — open (untouched):** final Brand placement.
- Carried, untouched: Q-015 (KNI-21); Q-016 (Client × Performance & Analytics View);
  the inherited Intelligence-Draft-v1 Open Questions (`domains.md:1915-1920`,
  incl. the insight→durable-knowledge threshold — G-7 carried, R-027).

---

## Assumptions
- Assumption: the Knowledge-ownership resolution is evidence-backed (Knowledge "What
  it owns"; the "strongest long-term ownership direction" prior wording; Q-001
  direction), not invented.
- Assumption: the grep-complete edit set (13 `domains.md` sites) leaves no surviving
  Q-001-style ownership-openness assertion (verifier-confirmed).
- Assumption: Q-004 / Q-003 / aggregate status remain genuinely open; the edits do
  not narrow them.

---

## Risks

### R-027 (carried forward, active — held)
No inherited Phase-1 question resolved: the G-7 insight→durable-knowledge threshold
(`domains.md:1917`) is carried, referenced at altitude in the pending Learnings
entry, not resolved.

### R-028 (carried forward, active — held)
Reference-altitude overcommitment: the experience-architecture.md citations are
refreshed as references, not reinterpretations; `memory.md` is not yet written, so
no Phase-5 content overcommits.

### R-029 (carried forward, active)
Authorization-slice bleed (from DEC-026). Untouched by this ownership work (DEC-026
access-only line held: Governance authorizes access; Knowledge owns). Keep active.

### R-025 / R-026 (carried forward, active)
R-025 close-out discipline applied (content read + double independent verifier pass
before landing; same-commit tracker backfill). R-026 sync-protocol tooling-name
watch unchanged.

### New watch (normalization backlog, not a numbered risk)
`CLAUDE.md` lists "final Client Brain ownership" as a draft-area example (Current
Project Direction; Normalization Rules). DEC-027 resolved it ("unless separately
resolved" caveat satisfied), so the example is now stale. Reconciling it is a GATED
procedure-file edit, deferred to the normalization pass.

---

## Next Focus
1. **Phase 5 `memory.md` first write (landing (ii)) — the next Phase 5 work.** Six
   firm sub-items (Client Brain, Agency Brain, Knowledge Base, Decision Logs,
   Learnings, Context Retrieval) on the DEC-027 six-field skeleton; Asset
   Intelligence a deferred-flagged stub. Client Brain entry written
   partition-agnostic (Q-004); Learnings references the insight→durable-knowledge
   threshold at altitude (R-027). A fresh gated content batch.
2. **Q-004** (Client Brain partitioning) — resolves with Q-003 Brand placement when
   scheduled; its own gated decision. Not blocking the `memory.md` write.
3. Carried: Q-016 (DEC-022 view membership); Q-015 (KNI-21); the four Phase-4
   deferred sub-items; the inherited Intelligence questions.
4. Normalization backlog: `CLAUDE.md` stale Client-Brain-ownership draft example
   (gated procedure edit).
5. Linear: minimal Phase 5 reconciliation (milestone/label + "Define scope" Done +
   "Write memory.md" Todo), mirroring Phases 2/3/4 — structural, pending Ali's
   approval.
