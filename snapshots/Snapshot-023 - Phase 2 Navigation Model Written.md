# Snapshot-023 - Phase 2 Navigation Model Written

## Current Phase
Phase 11 Claude Code Operating System — operational
Phase 2 Experience Architecture — active; Navigation Model section now
written into canon (this session)

## Current Topic
Canon write of the Navigation Model (KNI-17). Plan-then-write, composing a
surface-movement model over already-populated canon (the 16-surface
inventory, the per-persona Portal Contents, the composed-portal rule). No
new discovery, no new architecture, no new decision. One genuine open
question (entry/landing surface) was surfaced and resolved by human choice:
flat-peer movement, landing surface deferred.

## Status
The Navigation Model section of experience-architecture.md now defines the
surface-movement model: flat peer movement between the Operating Surfaces in
a persona's composed portal (no prescribed order, no gate); within-surface
drill to hosted Cross-Domain Views (a view is not a separate portal entry,
per DEC-022); composed-portal navigation across the union for multi-role
humans; movement within the persona's own portal only. The model is uniform
and applies to each persona's composed portal. Landing/entry surface,
ordering, and grouping are not assigned (deferred, marked future item);
entitlement stays deferred to the Permission Matrix; notification routing
and deep-linking (including contextual item-level navigation) stay deferred.
No sequence or gates (Phase 6), no channels (Phase 4). With this, Phase 2 is
5-of-7 canon sub-items populated. Repository is in a stable, confirmed state.

---

## Document Updates

### DOC-050
Faraz-OS-Canon/experience-architecture.md updated (single-file,
minimal-diff). Changes:
- §Navigation Model populated: Definition and "Scope in this pass"
  preserved; new "Movement model" (flat peer; within-surface view drill;
  composed-portal union; within own portal; uniform-per-portal note); new
  "What the Navigation Model does not decide" (landing surface / ordering /
  grouping deferred; entitlement deferred; contextual item-level navigation
  = deep-linking, deferred); "Deferred future sub-detail" extended with
  landing-surface designation; "What Navigation is not" extended (approval
  gates → Phase 6; not the Permission Matrix); new "Scope of definition in
  this file".
- Purpose and Status updated to record the Navigation Model as defined.
- Open and Deferred Items "Deferred — Navigation sub-detail" extended with
  landing-surface designation.
- Provenance: the Navigation Model derives from composing over the Operating
  Surfaces inventory (DEC-020) and the Portal Contents (DEC-021), per
  KNI-17, recorded in this snapshot.

### DOC-051
Current-State.md updated: Phase 2 is now 5-of-7 populated (Navigation Model
written); Channel Behaviors soft-blocked on Phase 4; Permission Matrix
blocked on Phase 1 Governance.

---

## Decisions
No new decisions this session. The Navigation Model was composed over
existing canon (DEC-020 inventory, DEC-021 portal contents); no new decision
was required. The single open question (entry/landing surface) was resolved
in favour of deferral, not a new architecture decision.

---

## Findings
No new findings this session.
- FIND-025 (Phase 2 4-of-7 status) is superseded in count by this write
  (now 5-of-7) but its substance — remaining work differentiated by state —
  stands; recorded here, not re-promoted.

---

## Open Questions
No new open questions this session.
- Q-011 resolved; Q-012 deferred (KNI-11); Q-013 deferred (repo-only);
  Q-014 deferred (KNI-16, Permission Matrix ↔ Phase 1 Governance).

---

## Assumptions
- Assumption: the Navigation Model composes only over existing canon; no
  surface, portal, view, rule, or flag was added or changed.
- Assumption: the model holds Phase 2 altitude — movement between surfaces,
  with no sequence or gates (Phase 6) and no channel integration (Phase 4).
- Assumption: landing-surface designation, ordering, and grouping remain
  deferred (marked future item), consistent with the Portals section.
- Assumption: entitlement remains deferred to the Permission Matrix; no
  Phase 1 domain truth was modified.

---

## Risks

### R-023 (carried forward, active, reduced)
Phase 2 may drift into Phase 3 / Phase 6 territory as sub-items are
populated. Mitigation held this session: flat-peer movement with explicit
"no sequence, no gate" and an "approval gates belong to Phase 6" exclusion
keep the navigation altitude. Keep active.

### R-025 (carried forward, active)
Session close-out is applied by discipline; the propose-then-review gate was
followed this session.

### R-026 (carried forward, active)
workflows/sync-protocol.md references Linear MCP tools by logical name;
update its Tooling section if those names change.

---

## Next Focus
1. Phase 2 is 5-of-7 populated. Remaining:
   - Channel Behaviors — framework-only, soft-blocked for per-channel
     population on the Phase 4 Channel Model (KNI-18, Canceled-with-reopen).
   - Permission Matrix — structure-defined, blocked on concrete Phase 1
     Governance rules (Q-014 / KNI-16; population work in KNI-14).
2. The landing-surface designation, notification routing, and deep-linking
   stay deferred Navigation sub-detail (marked future items).
3. The three Operating-Surface flags (Client Profile, System Configuration /
   Settings, Onboarding) stay parked.
4. Carry the Future Personas placeholder forward as a marked future item.
